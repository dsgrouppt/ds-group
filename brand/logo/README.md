# Logótipo

Ainda não existe logótipo final. `favicon-provisional.svg` é uma marca
temporária (texto "DS" sobre fundo preto) usada apenas para o favicon do
website enquanto o logótipo definitivo não é produzido.

Próximo passo: desenvolver graficamente o **Sistema Modular por Unidade de
Negócio** (conceito #19 em `../logo-concepts.md`), usando o wordmark do
conceito #7 como base tipográfica — ver secção "Recomendação estratégica"
nesse documento.

Quando o logótipo final existir, substituir aqui:
- `logo-full-black.svg` / `logo-full-white.svg` (versão com wordmark)
- `mark-black.svg` / `mark-white.svg` / `mark-gold.svg` (símbolo isolado)
- `favicon.svg` (e remover `favicon-provisional.svg`)
