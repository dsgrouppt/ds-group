# Variáveis para o docker-compose.yml (deploy auto-hospedado em VPS/Hetzner).
# Copiar para .env.docker e preencher com valores reais antes de:
#   docker compose --env-file .env.docker up -d --build

POSTGRES_USER=dsuser
POSTGRES_PASSWORD=troque-por-uma-password-forte
POSTGRES_DB=ds_os

NEXTAUTH_SECRET=gerar-com-openssl-rand-base64-32
PLATFORM_URL=https://os.dsgroup.pt

WEBSITE_URL=https://www.dsprojects.pt
NEXT_PUBLIC_GTM_ID=
NEXT_PUBLIC_GA_ID=
NEXT_PUBLIC_META_PIXEL_ID=
HUBSPOT_PORTAL_ID=
HUBSPOT_FORM_GUID=
